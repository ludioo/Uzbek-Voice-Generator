# Threads promo copy (G10)

Short drafts pointing at the GitHub Release `.exe`. Replace `YOUR_RELEASE_URL` with the real asset link (for example `https://github.com/ludioo/Uzbek-Voice-Generator/releases`) before posting.

---

## Draft 1 (EN)

Uzbek text → MP3 in one click.

Download **UzbekTTS.exe** (Windows, ~14 MB), paste Uzbek text, pick Male/Female, Generate. Batch CSV supported too.

Internet required (Edge TTS).
YOUR_RELEASE_URL

---

## Draft 2 (UZ / EN mix)

O‘zbek matnni MP3 ga aylantiring — CLI shart emas.

Windows uchun bitta fayl: **UzbekTTS.exe**. Single text + CSV batch. Chiqish: `output/` papkasi.

Release: YOUR_RELEASE_URL

---

## Draft 3 (feature-focused)

Built a small Windows app for Uzbek TTS:

- Single text + CSV batch
- Male / Female (Edge neural voices)
- One `.exe`, no Python install for end users

SmartScreen may warn (unsigned build) → More info → Run anyway if you trust the release.

YOUR_RELEASE_URL
